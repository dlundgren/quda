<?php $job = $this->job; ?>
<a data-toggle="confirm" data-action="delete" data-method="delete" href="<?= View::urlFor('job-delete', ['id' => (string)$job->id()]) ?>" class="btn btn-danger btn-xs">Delete</a>
